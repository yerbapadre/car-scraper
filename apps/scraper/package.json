{
	"name": "scraper",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types",
		"db:init": "wrangler d1 execute edmunds_users --local --file=./schema.sql",
		"db:init:remote": "wrangler d1 execute edmunds_users --file=./schema.sql"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.54.0"
	},
	"dependencies": {
		"@cloudflare/playwright": "^1.0.0"
	}
}